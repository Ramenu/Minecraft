#ifndef LINE_HPP
#define LINE_HPP

#if 0
    /* Returns the slope between the two points of the line. */
    inline float getSlope(const float x1, const float y1, const float x2, const float y2) {return (x2 - x1) / (y2 - y1);}

    /* Calculates the y-intercept with the formula: (b = y - m * x). */
    inline float getYIntercept(const float x, const float y, const float m) {return y - m * x;}
#endif

#endif // LINE_HPP
