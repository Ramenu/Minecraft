#ifndef MC_LINE_HPP
#define MC_LINE_HPP

#if 0
    /* Returns the slope between the two points of the line. */
    inline float getSlope(float x1, float y1, float x2, float y2) {return (x2 - x1) / (y2 - y1);}

    /* Calculates the y-intercept with the formula: (b = y - m * x). */
    inline float getYIntercept(float x, float y, float m) {return y - m * x;}
#endif

#endif // MC_LINE_HPP
